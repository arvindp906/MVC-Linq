@model IEnumerable<CrudOperation.VegeData>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VegetableName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VegetableType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VegetablePrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FarmerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductionState)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VegetableName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VegetableType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VegetablePrice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FarmerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductionState)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Veg_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Veg_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Veg_id })
        </td>
    </tr>
}

</table>
